这是单线程的程序，如果多个客户端同时向服务器上传文件的话，那么就会产生排队现象。即一个客户端传完，服务器接收完，然后才能开始接收下一个客户端传过来的信息。

这种情况并不好，因为假如前一个人要传一个100G的文件，网速又很慢，其他人也得一直等着。

比较合理的情况是，一个客户端连接，服务器就立马开一个线程接收。 然后大家同时上传。  大家都去抢服务器CPU的执行权。   网速快的人，不用等网速慢的人传完才能开始传。